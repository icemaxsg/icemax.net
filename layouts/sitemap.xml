{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- /* A sitemap asks search engines to index these URLs, so pages carrying
         noindex are excluded rather than contradicting themselves. Language
         alternates are declared inline, which is what Google reads for
         multilingual sites. */ -}}
  {{- range .Pages }}
    {{- if not .Params.noindex }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- if .IsTranslated }}
      {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Locale }}" href="{{ .Permalink }}" />
      {{- end }}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ (index hugo.Sites 0).Home.Permalink }}" />
    {{- end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
